# =============================================================================
# CareerOS — Production Environment Contract
# =============================================================================
# Copy to .env.local for development. Never commit real secrets.
# The /api/health readiness endpoint fails closed in production when core values
# are missing or malformed.
# =============================================================================

# --- App [REQUIRED] ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Supabase [REQUIRED] ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- AI [REQUIRED] ---
AI_PROVIDER=nvidia
NVIDIA_NIM_API_KEY=
NVIDIA_NIM_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_NIM_MODEL=meta/llama-3.3-70b-instruct
NVIDIA_NIM_MODEL_FAST=meta/llama-3.1-8b-instruct
ANTHROPIC_API_KEY=                         # optional fallback
OPENAI_API_KEY=                            # optional fallback

# --- Inngest [REQUIRED in production] ---
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# --- Upstash [REQUIRED in production] ---
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
RATE_LIMIT_SALT=                           # random value, minimum 16 chars in production

# --- Stripe [OPTIONAL, but configure all-or-none] ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_PRICE_ID=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=

# --- Observability [REQUIRED in production] ---
# The DSN is not a secret. Set both to the same Sentry project DSN to capture
# server and browser failures. SENTRY_AUTH_TOKEN is only needed for future
# source-map upload automation and must remain server-side.
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=

# --- Support ---
NEXT_PUBLIC_SUPPORT_EMAIL=support@amauralabs.com

# NODE_ENV is set by the deployment platform. Do not force production locally.
